def send_push_notification(target, title, message, metadata=None):
    # Placeholder: hook into FCM / OneSignal / local SMS provider
    print("SEND PUSH ->", target, title, message)
    return True
